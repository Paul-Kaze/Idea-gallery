version: '3.8'

services:
  web:
    build: .
    image: dommi-ai-web:latest
    container_name: dommi-ai-web
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env.production
    environment:
      - NODE_ENV=production
      - HOSTNAME=0.0.0.0
      - PORT=3000
